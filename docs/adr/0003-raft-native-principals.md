# Default to Raft-native principals

Eve Raft is a reusable channel rather than an account-linking product. It will identify actors with stable Raft principals by default and expose an optional consumer-owned principal resolver for applications that need their own identity mapping, keeping authorization and account semantics outside the package.
